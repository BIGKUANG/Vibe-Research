# 股票筛选页（/screener）页面布局设计

> 范围：旧版 Web 应用（`frontend/` + `backend/`，运行于 `http://localhost:8901`），在左侧导航「自选股」下方新增「股票筛选」入口。
> 数据来源参考：`a-stock-data/SKILL.md`（A股全栈数据工具包 v3.6.1，十层 47 端点）+ `docs/full-market-snapshot.md`（腾讯批量 + 巨潮代码表全市场快照方案）。
> 原则：只呈现客观公开数据与筛选结果，**不出现建仓/加减仓/目标价/止损位等任何投资动作建议**；所有派生比率/倍数经 `calc/` 计算，前端不手搓公式。
> 状态：本文为布局与实现设计，未改动任何代码。

---

## 一、定位与设计目标

「股票筛选」= 给全市场 A 股做**条件过滤 + 排序 + 结果导出**的工作台，和「自选股」（已确定标的的跟踪）互补，也和「板块中心」（行业级视角）互补。

设计目标按优先级：

1. **快**：默认只加载一层「全市场实时快照」，条件过滤与排序在浏览器本地完成，交互无网络等待（全市场约 5500 只 A 股，一次拉取即可，见 §四）。
2. **稳**：全市场快照只走腾讯批量（不封 IP）；MVP 唯一的额外增强是 ROE（mootdx 财务，TCP 不封），只对当前结果集限量拉取。东财系（资金流/龙虎榜等）本期不接入，留待后续扩展。
3. **省**：一层数据本地静态表（代码/名称/行业/地区/板块/上市日）已有，零网络成本。
4. **合规**：结果表只是数据，不出现买卖信号；情绪类（涨停/热榜）显式标注「情绪参考，不进核心结论」。

---

## 二、导航与入口

### 2.1 侧边栏插入位置

现有顺序（`frontend/src/components/layout/Layout.tsx:22-33`）：

```
每日复盘 → 资讯雷达 → 板块中心 → 个股数据 → 多空辩论 → 自选股 → 我的持仓 → 我的研报 → 研究记录 → 接入 AI
```

改为在「自选股」**下方**插入一条：

```ts
{ to: "/screener", icon: SlidersHorizontal, label: "股票筛选" },   // 紧随自选股
```

- 图标：`lucide-react` 的 `SlidersHorizontal`（多条件过滤语义，和个股数据的 `Search` 区分开）。
- 路由：`frontend/src/router.tsx` 增加 `{ path: "/screener", element: <Screener /> }`。
- 折叠态（`collapsed`）沿用现有 `title` 悬浮提示逻辑，无需特殊处理。

### 2.2 页面版式与容器宽度

筛选页采用**上下布局**：上半屏是筛选条件区（可折叠），下半屏是结果列表，整体单列、自上而下。不含左侧固定筛选栏。

现有 `<main>` 内容区是 `max-w-6xl`（`Layout.tsx:174`）。结果表列较多（默认 16 列），`max-w-6xl`（1152px）偏窄。建议二选一：

- **方案 A（推荐，改动小）**：给 `Layout` 的 `main` 内层容器按路由放宽 —— 当 `pathname === "/screener"` 时用 `max-w-[1600px]`，其余保持 `max-w-6xl`。
- **方案 B**：筛选页内部对结果表做横向滚动，表体用 `min-w-[1100px]` 包裹，筛选区仍铺满容器宽度。

上下布局的好处：筛选区宽度充裕，条件组可横向平铺成多列卡片，不必挤在窄侧栏里滚动；结果表也能占满整屏宽度。

---

## 三、页面整体布局

### 3.1 线框图（上下布局，展开态，桌面 ≥1280px）

```
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│ 股票筛选                                                                                     │
│ 全市场 A 股条件过滤 · 数据仅作客观呈现，不构成投资建议                                        │
├──────────────────────────────────────────────────────────────────────────────────────────────┤
│ 筛选条件                                                              已选 4 项   [收起 ▾]  │
│ ┌─ 市场板块 ────┐ ┌─ 行业 ────────┐ ┌─ 估值 ─────────┐ ┌─ 市值范围（总市值） ────────┐   │
│ │ ☑沪深北 全选  │ │ [白酒 ×][电池×]│ │ PE(TTM) [ ][ ] │ │ 总市值(亿)   [    ]–[    ]  │   │
│ │ ☑主板  2278   │ │ 搜索行业…     │ │ ☑仅正 PE       │ │ 快捷档位:                    │   │
│ │ ☑创业板 1406  │ │ [选择行业 ▾]  │ │ PB      [ ][ ] │ │ [≤50][50-100][100-300]      │   │
│ │ ☑科创板  616  │ │               │ │ ROE(%)  [ ][ ] │ │ [300-1000][≥1000] 亿        │   │
│ │ □北交所  343  │ │               │ │                │ │                              │   │
│ └───────────────┘ └───────────────┘ └────────────────┘ └──────────────────────────────┘   │
│ ┌─ 行情 ────────────────────────────────────────────────────────────────────────────────┐ │
│ │ 涨跌幅% [ ][ ]   换手率% [ ][ ]   量比 [ ][ ]   成交额(万) [ ][ ]   振幅% [ ][ ]       │ │
│ └────────────────────────────────────────────────────────────────────────────────────────┘ │
│                                                                   [重置]   [ 开始筛选 ]     │
├──────────────────────────────────────────────────────────────────────────────────────────────┤
│ 命中 3,482 只 · 快照 2026-09-13 15:00:03 · ROE 已加载                                        │
│ [列设置▾] [导出 CSV] [刷新]                                                                  │
├──────────────────────────────────────────────────────────────────────────────────────────────┤
│ 名称/代码        现价    涨跌%   换手%   量比    PE(TTM)  PB     总市值    行业     操作      │
│ ────────────────────────────────────────────────────────────────────────────────────────────│
│ 贵州茅台 600519  1275.16  -0.78   0.28    1.25    19.57   6.34   15941亿   白酒     ☆ 查看   │
│ 宁德时代 300750    xxx    +2.10   ...     ...      ...     ...     ...      电池     ☆ 查看   │
│ …（虚拟滚动，行高 40px）                                                                     │
├──────────────────────────────────────────────────────────────────────────────────────────────┤
│ [1] 2 3 4 … 174   每页 [50 ▾]                  免责声明：不荐股 · 不预测 · 无倾向             │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
```

- 上栏筛选区固定高度、超出时内部折叠；点「收起」只留一行已选条件摘要，把空间让给结果表。
- 结果表是唯一主滚动区；筛选区不随结果表滚动（`sticky` 或独立滚动容器）。
- 窄屏（<900px）：筛选条件卡片由多列自动降为单列/两列，结果表横向滚动。
- **市值范围**单列一张卡片，只筛**总市值**：一个双输入框 + 一排快捷档位（≤50 / 50–100 / 100–300 / 300–1000 / ≥1000 亿），点档位即填入区间。单位统一「亿元」。
- **估值**卡片展示 PE(TTM)、PB、ROE 三项；PE/PB 为 L1 即时可用，ROE 属 L2 指标，按需对当前结果集限量拉取（MVP 唯一的增强项）。已移除 PE(静)。
- **「更多条件」小组件本期不做**（财务质量/一致预期/资金筹码/技术形态/事件情绪等深度条件暂不进入页面，设计方案保留在 `§4.3 后续扩展条件`）。

### 3.2 区块职责（自上而下）

| 区块 | 组件 | 职责 |
|------|------|------|
| A 页头 | `PageHeader` | 标题、副标题 |
| B 筛选条件区（上） | `ScreenerFilters` | 横向平铺的条件组卡片：市场板块、行业、估值、市值范围、行情；底部操作栏「重置 / 开始筛选」。区间输入、多选标签，改动写入草稿，点「开始筛选」才应用 |
| C 结果工具栏 | `ScreenerToolbar` | 命中数、快照时间戳、ROE 加载状态、列设置、导出、刷新；「收起筛选」按钮也放这里 |
| D 结果列表（下） | `ScreenerTable` | 可排序列、虚拟滚动、行内动作（加自选/看个股） |
| E 行详情抽屉 | `StockDrawer`（复用/新写） | 点行展开该股关键数据摘要，不跳页打断筛选 |
| F 页脚 | `Disclaimer` | 合规声明 |

### 3.3 结果表默认列

| 列 | 字段 | 来源层 |
|----|------|--------|
| 名称/代码 | `name` / `code` | L0 本地 |
| 现价 | `price` | L1 腾讯 |
| 涨跌% | `change_pct` | L1 腾讯 |
| 换手% | `turnover_pct` | L1 腾讯 |
| 量比 | `vol_ratio` | L1 腾讯 |
| 振幅% | `amplitude_pct` | L1 腾讯 |
| PE(TTM) | `pe_ttm` | L1 腾讯 |
| PB | `pb` | L1 腾讯 |
| ROE(%) | `roe` | L2 按需（对当前结果集批量） |
| 总市值 | `mcap_yi` | L1 腾讯 |
| 流通市值 | `float_mcap_yi` | L1 腾讯 |
| 成交额 | `amount_wan` | L1 腾讯 |
| 行业 | `industry` | L0 本地 |
| 地区 | `area` | L0 本地 |
| 板块 | `market` | L0 本地 |
| 上市日 | `list_date` | L0 本地 |

> 后续扩展列（前瞻PE / PEG / 主力净流入 / 龙虎榜等）随 `§4.3` 的后续条件一并规划，本期默认列不含。

「列设置」允许勾选/隐藏列，选择存 `localStorage`（键 `vr-screener-columns`）。

### 3.4 排版原则（简洁美观）

1. **一屏两段，主次分明**：上半屏是「输入」（筛选条件），下半屏是「输出」（结果列表）；视觉重心落在结果表，筛选区永远不与结果表抢焦点。
2. **数字优先、去噪**：所有数字列用等宽字体（`font-mono`）+ `tabular-nums` + 右对齐，读表像看行情软件一样对齐；涨跌只用红/绿两种颜色，其余标签一律中性灰，避免整屏花。
3. **留白成节奏**：沿用现有 8px 间距体系（`gap-3` / `mb-4` / `p-4`），卡片之间固定 12px，区块之间 16–24px，不靠分割线堆砌。
4. **沿用现有设计语言**：容器统一用 `GlassCard`（`glass` 玻璃拟态：半透明填充 + 发丝边框 + 顶部内高光），主色沿用暖橙 `--primary`；不引入新色板、新圆角、新阴影。
5. **交互反馈轻量**：悬停才出现的行内操作、输入聚焦时边框转主色、加载用骨架条而非转圈，保持画面安静。
6. **克制用色**：主色只用于「选中态 / 焦点 / 可点强调」，面积小；大面积是 `background` / `card` / `muted` 的中性层次。

### 3.5 栅格与间距

| 区域 | 规范 |
|------|------|
| 页面容器 | 沿用 `Layout` 的 `px-6 py-6`，筛选页放宽到 `max-w-[1600px]`（见 §2.2） |
| 页头 | `PageHeader` 默认，`mb-6` |
| 筛选卡容器 | `GlassCard` + `p-4`（比默认 `p-5` 略紧，提高信息密度），`mb-4` |
| 筛选卡栅格 | CSS `grid grid-cols-12 gap-3`：市场板块 / 行业 / 估值 / 市值范围 各 `col-span-3`；行情整行 `col-span-12`，内部 5 个区间横向 `flex gap-3` 等分 |
| 结果卡容器 | `GlassCard` + `p-0`（内部工具条与表格自带内边距），与筛选卡间隔 `mb-4` |
| 表格单元格 | 左右 `px-3`，行高 `h-10`，表头 `h-9` |

响应式（`§3.9` 详列）：`≥1440` 四卡一行 → `1024–1440` 两卡一行 → `<1024` 单列。

### 3.6 筛选区视觉规范

- **卡片标题**：`text-xs font-medium text-muted-foreground`，前缀一个 3px 高、`bg-primary/70` 的圆角竖条（或用 14px 线性图标），标题与内容间距 `mb-2.5`。
- **输入控件**：统一高度 `h-8`，`rounded-md border border-border bg-black/20 px-2.5 text-sm outline-none transition-colors focus:border-primary/50`（与自选股页 textarea 同款）。
- **区间输入**：`[min] – [max]`，两个 `input` 各 `w-20 text-right font-mono`，中间用 `text-muted-foreground` 的「–」分隔；空值显示占位符「不限」（`placeholder:text-muted-foreground/40`）。
- **快捷档位 chips**：`rounded-full border border-border/60 px-2.5 py-1 text-xs`；悬停 `hover:border-primary/40 hover:text-foreground`；选中 `border-primary/50 bg-primary/10 text-primary`。市值档位一行 5 个，超出换行。
- **多选标签**：已选项为 `rounded-full bg-primary/10 px-2 py-0.5 text-xs text-primary`，尾部 `×` 按钮；下方是搜索框 + 复选列表（最多 `max-h-40 overflow-auto`）。
- **开关**：排除 ST / 仅正 PE / 排除僵尸报价，沿用现有 toggle 风格，右侧贴齐。
- **操作栏（筛选卡底部，右对齐）**：
  - **开始筛选**（主按钮）：`inline-flex h-9 items-center gap-1.5 rounded-lg bg-primary/15 px-5 text-sm font-medium text-primary shadow-glow transition-colors hover:bg-primary/25 disabled:opacity-40`。点击后提交草稿条件 → 应用过滤 → 把结果表滚到顶部；**筛选区保持展开，不自动折叠**（是否收起由用户点头部「收起」自行决定）。
  - **重置**（次按钮）：`h-9 rounded-lg border border-border/60 px-4 text-sm text-muted-foreground transition-colors hover:border-border hover:text-foreground`。一键清空所有条件回到默认（默认开关保持：排除僵尸=开、排除 ST=开），并立即重新筛选。
  - 两按钮间距 `gap-3`，与上方卡片间距 `mt-3`；筛选卡头部右侧显示「已选 N 项」与「收起/展开」图标按钮（`ChevronUp / ChevronDown`，`text-muted-foreground hover:text-foreground`）。
- **收起态**：筛选卡收成一行「已选条件摘要」（每条件一个中性 chip，未选显示「未设置条件」）+ 右侧「展开筛选」。摘要行 `h-12`，把纵向空间让给结果表：

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ 已选: 主板 创业板 · 白酒 电池 · PE 0–30 · 总市值 100–1000亿      [展开筛选 ▾] │
├──────────────────────────────────────────────────────────────────────────────┤
│ 命中 3,482 只 · 快照 15:00:03 · ROE 已加载        [列设置▾] [导出 CSV] [刷新] │
│ 名称/代码        现价     涨跌%   换手%   PE(TTM)  PB     ROE    总市值  操作 │
│ 贵州茅台 600519  1275.16  -0.78   0.28    19.57   6.34   28.1   15941亿  ☆…  │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 3.7 结果区视觉规范

- **工具条**（表格上方一行，`flex items-center justify-between`）：
  - 左侧：命中数 `text-sm font-medium`；快照时间 `font-mono text-xs text-muted-foreground`，过旧时转 `text-warning`；ROE 状态 `text-xs`（加载中显示脉冲点 + 「ROE 加载中」，完成显示「ROE 已加载」，失败显示原因）。
  - 右侧：操作按钮组 `h-8 text-xs`，ghost 风格（`text-muted-foreground hover:text-foreground`），主操作「导出 CSV」可用 `bg-primary/15 text-primary`。
- **表头**：`sticky top-0 z-10 bg-card/95 backdrop-blur`，`text-xs text-muted-foreground`；可排序列悬停显示升降序箭头，激活列箭头与文字转 `text-primary`。
- **数据行**：`h-10 border-b border-border/30 transition-colors hover:bg-muted/40`；不做斑马纹，靠悬停区分，保持干净。
- **列对齐**：名称/代码左对齐（名称 `font-medium`，代码 `font-mono text-xs text-muted-foreground` 换行或紧随）；所有数值列右对齐 `font-mono tabular-nums`。
- **涨跌配色**（A股红涨绿跌）：`change_pct > 0 → text-danger`、`< 0 → text-success`、`= 0 → text-muted-foreground`；正数带 `+` 前缀，全表统一，不额外加背景色块。
- **标签**：市场/行业等文本列用 `text-muted-foreground`；ST/停牌等风险标记用 `rounded bg-warning/10 px-1.5 text-[11px] text-warning`。
- **ROE 列**：加载中显示骨架条（`h-3 w-12 animate-pulse rounded bg-muted/60`），失败/无数据显示「—」并带 `title` 说明。
- **行内操作**：默认 `opacity-0`，行 `hover` 时 `opacity-100`，两个图标按钮「☆ 加入自选」「↗ 查看个股」；键盘聚焦时也显示。
- **分页**：`flex items-center justify-between`，页码当前项 `rounded bg-primary/15 px-2.5 py-1 text-primary`，其余 `text-muted-foreground hover:text-foreground`；每页下拉 `h-8 text-xs`。

### 3.8 配色与状态 token 对照

全部复用现有 `frontend/src/index.css` 与 `tailwind.config.js` 的 token，不新增变量：

| 语义 | token / class |
|------|----------------|
| 页面底色 / 正文 | `bg-background` / `text-foreground` |
| 卡片容器 | `.glass`（`GlassCard`） |
| 次级文字 / 占位 | `text-muted-foreground` / `placeholder:text-muted-foreground/40` |
| 边框 / 分隔 | `border-border`、`border-border/50` |
| 主色（选中/焦点/强调） | `text-primary`、`bg-primary/10`、`border-primary/50` |
| 涨 | `text-danger` |
| 跌 | `text-success` |
| 警示（停牌/ST/数据过旧） | `text-warning`、`bg-warning/10` |
| 骨架 / 禁用 | `bg-muted/60`、`text-muted-foreground/40` |
| 阴影 / 光晕 | `shadow-glow`（仅焦点与主按钮，克制使用） |

### 3.9 响应式断点

| 断点 | 表现 |
|------|------|
| ≥1440px | 筛选四卡一行（各 3 列）+ 行情整行；表格展示默认 16 列 |
| 1024–1440px | 筛选两卡一行（各 6 列）；结果表保底 `min-w-[1080px]` 横向滚动 |
| <1024px | 筛选单列堆叠；工具条换行；结果表横向滚动，隐藏次要列（换手/量比/振幅/流通市值/上市日），保留 名称、现价、涨跌、PE、PB、ROE、总市值、行业、操作 |
| <640px | 筛选区间输入改为单列；分页简化为「上一页 / 页码 / 下一页」 |

---

## 四、数据分层与筛选维度

### 4.1 三层数据架构

```
L0 本地静态表  frontend/src/data/stock_codes.csv（code,name,industry,area,market,list_date）
               A股 5562 只（主板2278/中小板919/创业板1406/科创板616/北交所343），零网络
        ↓
L1 全市场实时快照  GET /api/screener/snapshot（后端内存缓存，腾讯 qt.gtimg 批量，≤500 只/块）
               ~5500 行 × 20 列，2 秒级；不封 IP
        ↓（按需，仅对当前结果集限量）
L2 按需增强（MVP）  ROE 财务快照（mootdx finance），仅对当前结果集限量拉取
        ↓（本期不做，留待后续扩展）
L2+ 其它增强  一致预期(同花顺) / 个股资金流(东财) / 板块归属(东财) / 龙虎榜·解禁·两融(东财)
        ↓
L3 市场级榜单 行业排名(现有 /api/industry) / 板块资金流(§3.8) / 涨停四池(§8.1) / 热榜人气榜(§10.2)
```

**核心判断：筛选的「主力维度」放在 L1**。因为腾讯批量快照零鉴权、不封 IP、可高频、覆盖全市场，且已含价格/涨跌/换手/量比/振幅/PE/PB/市值/成交额/涨跌停/证券类型。MVP 唯一的 L2 增强是 **ROE**（mootdx 财务快照，TCP 不封），只对「L1 过滤后的候选集」限量拉取。其余深度条件（一致预期/资金/技术/情绪）本期不做，避免全市场逐只调用东财被封（`SKILL.md` §数据源优先级，实测 10 线程裸调用 45000 次导致 IP 封 20+ 小时）。

### 4.2 筛选条件组 × 数据来源映射

| 条件组 | 具体条件 | 数据层 | a-stock-data § | 后端能力 |
|--------|----------|--------|----------------|----------|
| 市场板块 | 主板/中小板/创业板/科创板/北交所；全选/反选 | L0 | 本地 CSV | 零成本 |
| 行业 | 多选行业；关键词搜行业 | L0 | 本地 CSV | 零成本 |
| 上市时间 | 上市日区间、上市满 N 年、次新股 | L0 | 本地 CSV | 零成本 |
| 名称净化 | 排除 ST/*ST/退、排除「N/C/U」新股标识 | L0 名称字段 | — | 本地字符串 |
| 行情 | 现价区间、涨跌幅区间、振幅区间、换手率区间、量比区间、成交额区间 | L1 | §1.2 `tencent_quote` | 腾讯批量 |
| 估值 | PE(TTM) 区间（含「仅正 PE」开关）、PB 区间 | L1 | §1.2 | 腾讯批量 |
| 估值·ROE（按需） | ROE 区间 | L2 | §6.1 mootdx `finance`（含 `roe` 字段） | 需新增 astock 函数，对结果集限量 |
| 市值范围 | **总市值区间**、快捷档位（≤50 / 50–100 / 100–300 / 300–1000 / ≥1000 亿） | L1 | §1.2 字段 45=总市值 | 腾讯批量 |
| 交易状态 | 是否涨停/跌停（现价≈涨停价/跌停价）、排除僵尸报价 `is_stale`、排除停牌 | L1 | §1.2 `is_stale` | 腾讯批量 |
| 证券类型 | 仅股票（剔除 ETF/指数/可转债） | L1 | §1.2 字段 61 `security_type` | 腾讯批量 |

### 4.3 后续扩展条件（本期不做，方案留存）

以下条件组对应原计划的「更多条件」区块，功能需求已确认本期不需要，暂不进入页面；待后续需要时按下表接入。

| 条件组 | 具体条件 | 数据层 | a-stock-data § | 后端能力 |
|--------|----------|--------|----------------|----------|
| 财务质量 | 净利润区间、营收区间、EPS 区间、每股净资产 | L2 | §6.1 mootdx `finance`（37 字段） | 需新增 astock 函数，逐只限量 |
| 一致预期 | 有机构覆盖、预测机构数 ≥N、前瞻 PE 区间、PEG 区间、EPS 增速区间 | L2 | §2.2 同花顺 `ths_eps_forecast` | 现有 `/api/valuation` 逻辑可复用，逐只限量 |
| 资金筹码 | 主力净流入（今日）、近 20 日主力净流入、融资余额、股东户数环比 | L2 | §3.4/§4.5、§4.1、§4.3 | 需新增 astock 函数，**东财限流** |
| 技术形态 | 站上 MA20、均线多头、距 120 日高点/低点、连续 N 日放量 | L1 + K 线 | §1.3 百度带 MA / 本地 K 线 | 需新增批量 K 线函数，只对候选集 |
| 事件情绪 | 近 30 日龙虎榜、未来 90 天解禁、热门概念命中、人气榜 | L2/L3 | §3.5/§3.6、§3.3、§10.2 | 已有单票接口，逐只限量 |
| 市场级筛选 | 所属行业当日排名前 N、行业资金净流入为正 | L3 | §3.7/§3.8 | 已有 `/api/industry` |

### 4.4 筛选项交互控件选型

| 条件形态 | 控件 | 备注 |
|----------|------|------|
| 区间（PE/PB/ROE/涨跌/换手/量比/成交额） | 双输入框（min – max）+ 快捷档位 chips | 输入空 = 不限；非法/负值按口径处理；ROE 按需加载 |
| 市值范围 | 总市值双输入框（min – max）+ 快捷档位 chips | 档位点选即填入区间，可再微调；单位亿元 |
| 多选（市场/行业） | 复选 + 搜索 + 「已选标签 ×」 | 市场板块**默认勾选「主板」**；行业列表来自 L0 去重 |
| 布尔（排除 ST / 仅正 PE / 排除僵尸） | 开关 | 默认：排除僵尸报价 **开**，排除 ST **开** |

> **市值口径提示（数据纪律）**：市值筛选只按**总市值**（腾讯字段 45）。结果表另列流通市值（字段 44）只作展示、不参与筛选，两者不可混用（`SKILL.md` §1.2 踩坑提醒二：科创板/次新股/含限售股的票两者可差数倍）。单位一律「亿元」，由后端归一，前端不换算。

---

## 五、后端接口设计

### 5.1 第一层（必做）

```
GET /api/screener/snapshot?markets=main,gem,star,bj
```

- 后端：一次拉起全市场代码表（`frontend/src/data/stock_codes.csv` 或服务端同源副本）→ 腾讯批量（分块 500，并行度但腾讯不封）→ 解析 88 字段 → 内存缓存（TTL 30–60s）→ 返回紧凑 JSON。
- 复用 `backend/astock.py` 现有 `get_prefix` / `_fetch_gtimg` / `_parse_gtimg` / `tencent_quote`（`astock.py:94`），**无需另起模块**。
- 返回体建议紧凑列式（减少体积，利于前端本地过滤）：

```jsonc
{
  "as_of": "2026-09-13T15:00:03+08:00",
  "count": 5483,
  "fields": ["code","name","price","change_pct","turnover_pct","vol_ratio","amplitude_pct",
             "pe_ttm","pb","mcap_yi","float_mcap_yi","amount_wan",
             "limit_up","limit_down","security_type","is_stale"],
  "rows": [["600519","贵州茅台",1275.16,-0.78,0.28,1.25,1.80,19.57,6.34,15940.54,15940.54,443084,1413.64,1156.62,"GP-A",false], ...]
}
```

- 体积估算：约 5500 行 × 16 字段，紧凑数组 JSON ≈ 1.2–1.7 MB；开启 `GZipMiddleware` 后 ≈ 300–400 KB，一次拉取可接受。
- 前端 `ScreenerTable` 用虚拟滚动（`@tanstack/react-virtual` 或轻量自实现）渲染，避免 5000 行 DOM。

### 5.2 第二层（按需增强：ROE，及后续扩展位）

```
GET /api/screener/enrich?codes=600519,000858,...&groups=roe
```

- MVP 只实现 `groups=roe`：mootdx `finance`（§6.1，TCP 不封）取 `roe` 字段，按 `code` 归并后前端 merge 进结果行。
- `codes` 上限（建议 50）由前端截断，超限返回提示「ROE 增强最多 N 只，请先收窄条件」。接口与 `groups` 参数预留扩展位（`estimates` / `fundflow` 等本期不实现）。

### 5.3 市场级（后续扩展）

复用现有：`/api/industry`（行业排名）、`/api/market/ths-hot`（热榜）、`/api/market/emotion`（情绪）、`/api/market/turnover-top`（成交额榜）。
后续新增（东财限流）：`/api/screener/limit-up-pool`（§8.1 涨停四池）、`/api/screener/board-fund-flow`（§3.8 板块资金流）。本期不接入页面。

### 5.4 代码/组件结构（沿用仓库惯例）

| 文件 | 变更 |
|------|------|
| `backend/astock.py` | 新增 `screener_snapshot(codes)`（腾讯批量，缓存放 app 层）、`screener_roe(codes)`（mootdx finance）；沿用 `DependencyMissing` 惰性依赖 |
| `backend/app.py` | 注册 `/api/screener/snapshot`、`/api/screener/enrich`（MVP 仅 `groups=roe`；含 `_validate`、TTL 缓存、错误处理） |
| `frontend/src/lib/api.ts` | 增加 `ScreenerSnapshot`、`ScreenerRow`、`screenerApi.snapshot()` / `.enrich()` 类型与方法 |
| `frontend/src/pages/Screener.tsx` | 页面主组件（新增） |
| `frontend/src/components/screener/` | `ScreenerFilters.tsx` / `ScreenerToolbar.tsx` / `ScreenerTable.tsx` / `RangeInput.tsx` / `MultiSelect.tsx`（新增） |
| `frontend/src/components/layout/Layout.tsx` | NAV 插入「股票筛选」+ `/screener` 容器放宽 |
| `frontend/src/router.tsx` | 增加路由 |

### 5.5 调用路径与 3 秒预算

数据源映射、调用时序、耗时拆解与降级策略详见独立文档 **[stock-screener-api-call-plan.md](stock-screener-api-call-plan.md)**。要点：

- MVP 只需 `§1.2 tencent_quote`（关键路径，全市场分块 500 批量）+ `§6.1 mootdx finance`（ROE，异步）+ `§7.1 巨潮代码表`（后台刷新，可选）；不接入东财。
- 冷启动服务端 1.9–2.4s、热缓存 <50ms、前端本地过滤 <50ms，整体满足「结果 ≤3s」。
- 落地前需先修 `astock.py` 的 4 个既有问题：`get_prefix` 北交所 920 号段判定、`security_type`（字段 61）、`is_stale` 解析、腾讯分块。

---

## 六、交互与状态设计

### 6.1 过滤流程

采用**显式提交**：编辑条件不立即影响结果，点「开始筛选」才应用。

```
进入页面
  → 拉 /api/screener/snapshot（内存缓存命中则秒回）
  → 与 L0 本地表按 code 合并（行业/地区/板块/上市日），按默认条件先展示一次

编辑条件（写入 draftConditions，当前结果不变）
  → 点「开始筛选」：draftConditions → appliedConditions，纯本地 useMemo 过滤并重算命中集（无网络请求）
  → 点「重置」：清空草稿与已生效条件，回到默认（排除僵尸=开、排除 ST=开）并立即重筛

命中集变化
  → debounce 400ms 后拉 /api/screener/enrich?groups=roe（限 50 只），结果 merge 进结果表
  → 排序 / 分页 / 列设置：纯前端
```

### 6.2 关键状态

- `draftConditions`：编辑中的草稿条件，仅本地 state，点「开始筛选」前不影响结果。
- `appliedConditions`：已生效条件，驱动过滤；序列化后存 `localStorage`（键 `vr-screener-conditions`），刷新/重进保留。
- `filterDirty`：草稿与已生效条件不一致时为真，「开始筛选」呈高亮可用；一致时可置灰提示无改动。
- `enrichCache`：`Map<code, enrichData>`，会话内缓存，避免重复请求。
- `staleGuard`：请求竞态守卫 `runIdRef`，与 `StockData.tsx` 现有模式一致，旧响应不覆盖新结果。
- `snapshotAsOf`：快照时间戳，展示在工具栏；数据过旧（>N 分钟）时高亮提示，不静默。

### 6.3 行内动作（不含投资建议）

| 动作 | 行为 |
|------|------|
| 加入自选 | 调 `addCodes` 写入本地自选（复用 `lib/watchlist.ts`） |
| 查看个股 | 跳 `/stock-data?code=xxx`，个股数据页读取 `code` 参数并自动查询该股 |
| 导出 | 当前筛选结果导出 CSV（纯前端 Blob，字段与列设置一致） |

---

## 七、性能与防封

| 风险 | 措施 |
|------|------|
| 全市场快照体积大 | 后端 TTL 缓存 + GZip + 紧凑数组；前端虚拟滚动 |
| 腾讯限流（连续 5000+ 次返回空） | 只在刷新时拉取（默认 60s 内不重复请求），分块 500，不轮询 |
| 东财封 IP（实测 10 线程 45000 次封 20+ 小时） | 本期不接入东财，从源头规避；后续扩展时须对 ≤50 只候选集串行 `em_get`、每只间隔 ≥1s、复用会话 |
| mootdx 逐只财务慢 | 只对当前结果集（≤50 只）、并发受控（TCP 可小幅并发），结果会话缓存 |
| 页面卡顿 | 5000 行不一次性渲染；过滤用 `useMemo` + 预建索引（如按市场分桶），避免每次全量扫描 |

---

## 八、降级与合规

### 8.1 降级策略（必须在 UI 出声）

- 快照接口失败：保留上一次好数据并标注「数据可能过期」，不展示空表冒充成功；无缓存则显示错误态 + 重试。
- 僵尸报价：`is_stale` 的行默认剔除，若用户关闭该过滤，行内加「停牌/废码」灰标。
- ROE 增强失败：失败标的的 ROE 列显示「—」并给原因（如「mootdx 暂不可用」），其余行照常。
- 北交所老号段：按 `SKILL.md` §1.2 提示，只信巨潮代码表的现行码，避免静默拿到另一只股票数据。

### 8.2 合规红线（对齐 `AGENTS.md` §0）

- 结果只含**数据、筛选条件、排序**，不含「买入/卖出/持有/目标价/止损」等任何动作建议。
- 情绪类维度（涨停、热榜、龙虎榜）统一标注「情绪参考，不进核心结论」。
- 一切派生比率/倍数（前瞻 PE、PEG、同比、占比、分位）经 `calc/` 计算，禁止前端心算或手搓公式。
- 不接入需要密钥的源（如 iwencai，§2.3），维持 Phase 0 零鉴权边界。

---

## 九、分期实施建议

| 批次 | 内容 | 成本 | 依赖 |
|------|------|------|------|
| 第一批 MVP | 导航+路由+页面排版；L0 本地合并；L1 全市场快照；市场/行业/估值(PE/PB/ROE)/市值范围/行情/交易状态筛选；排序、分页、导出、加自选 | 中 | 腾讯批量（`astock.py` 已有解析）+ `screener_roe`（mootdx） |
| 第二批 | 行详情抽屉；ROE 增强的加载态与降级打磨 | 低 | — |
| 第三批（后续，本期不做） | 「更多条件」深度筛选（财务/一致预期/资金筹码/技术形态/事件情绪）、市场级榜单辅助 | 中高 | 东财限流链路 + 批量 K 线 + `calc/` 前瞻 PE/PEG |
| 第四批（后续，本期不做） | 列设置、自定义视图分享、快照自动刷新开关、性能压测 | 低 | — |

---

## 十、验收要点（自查清单）

1. 侧边栏「股票筛选」在「自选股」正下方，展开/折叠态均正常，激活态高亮。
2. 首屏只依赖 L1 快照即可完成「市场+行业+PE/PB+市值范围+换手+涨跌」全部筛选，本地交互无网络等待；ROE 按需加载且请求数受控（≤50 只）。
3. 筛选区底部有「重置」「开始筛选」两个按钮：编辑只改草稿，点「开始筛选」才应用并滚动到结果，**不自动折叠筛选区**；「重置」清空并回到默认条件立即重筛。
4. 页面为上下布局：筛选区在上、结果列表在下，符合「简洁美观」排版规范（见 §3.4–§3.9）。
4. 快照时间戳、数据层标识、降级原因在 UI 可见；失败不伪装成功、不用旧值冒充。
5. 结果表所有派生数字标注 `calculation_id`（或前端仅做展示、不做运算）。
6. 全页无任何投资动作建议措辞；情绪类数据带「情绪参考」标注。
7. 导出的 CSV 与当前筛选结果、列设置一致。

---

## 附：与现有页面/文档的关系

- `docs/full-market-snapshot.md`：本页 L1 数据的完整实现原型（腾讯批量 + 巨潮代码表 + 88 字段标定），可直接移植进 `astock.py`。
- `docs/stock-data-page-enhancement-suggestions.md`：个股页（`/stock-data`）的增强建议；本页行内「查看个股」直接跳转过去，二者共享数据源，不重复造轮子。
- 现有 `frontend/src/data/stock_codes.ts` / `stock_codes.csv`：本页 L0 宇宙与行业/地区分类来源。
